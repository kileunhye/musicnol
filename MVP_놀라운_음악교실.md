# 놀라운 음악 교실 MVP 정의서

## 추가 확정 범위: 계정 분리와 가사 싱크

MVP에서도 교사와 학생은 서로 다른 접근 계정과 화면을 사용한다. 학생이 단순히 교사 화면의 다른 모드로 들어가는 구조는 사용하지 않는다.

### 교사 MVP 흐름

1. 교사 계정 로그인
2. 교사 관리 화면 진입
3. MP3 업로드
4. 가사 입력 또는 붙여넣기
5. 음악을 재생하면서 가사 줄별 시작 시점 설정
6. 싱크 미리보기 및 저장
7. 문제와 정답 설정
8. 수업 세션 생성 및 학생 접근 코드 발급
9. 수업 시작·일시정지·종료와 결과 확인

### 학생 MVP 흐름

1. 학생 계정 로그인 또는 학생용 코드 입력
2. 허용된 학급/세션 선택
3. 학생 대기실 입장
4. 음악과 동기화된 가사 확인
5. 정답 제출
6. 본인 점수 및 세션 결과 확인

### 반드시 분리할 화면

- `/teacher/login`: 교사 로그인
- `/teacher/library`: 음악·가사 콘텐츠 관리
- `/teacher/lyrics/:id`: 가사 싱크 설정
- `/teacher/session/:id`: 수업 진행 및 결과
- `/student/login`: 학생 로그인/접근
- `/student/session/:id`: 학생 게임 화면

### 가사 싱크 설정 완료 조건

- [ ] 교사가 가사를 여러 줄로 입력할 수 있다.
- [ ] MP3 재생 중 현재 재생 시간이 표시된다.
- [ ] 버튼을 눌러 현재 시간에 가사 줄의 시작 시점을 기록할 수 있다.
- [ ] 가사 줄별 시작 시점을 직접 수정할 수 있다.
- [ ] 현재 재생 중인 가사가 강조된다.
- [ ] 저장한 싱크 데이터가 학생 화면에서 동일하게 재생된다.

### MVP 권한 완료 조건

- [ ] 교사 로그인 없이는 교사 관리 화면에 접근할 수 없다.
- [ ] 학생 로그인 없이는 학생 게임 화면에 접근할 수 없다.
- [ ] 학생은 MP3·가사·문제를 수정할 수 없다.
- [ ] 학생은 자신에게 허용된 세션에만 참여할 수 있다.

## 1. MVP 목표

교사가 한 곡으로 퀴즈 세션을 만들고, 학생들이 세션 코드로 입장해 음악을 듣고 정답을 제출하며, 교사가 실시간 응답과 최종 점수를 확인하는 최소 기능을 검증한다.

검증할 가설은 다음과 같다.

> 초등 음악 수업에서 음악 클립과 실시간 퀴즈를 결합하면 학생의 자발적인 응답 참여가 높아지고, 교사는 수업 이해도를 즉시 확인할 수 있다.

## 2. MVP 사용자 경험

### 교사

1. 교사 모드에서 세션을 생성한다.
2. MP3 1개를 업로드한다.
3. 곡 제목, 재생 구간, 정답, 오답 3개, 제한 시간 1개를 입력한다.
4. 생성된 세션 코드를 학생에게 공유한다.
5. 학생이 대기실에 들어오면 시작 버튼을 누른다.
6. 음악 클립을 재생하고 응답 현황을 본다.
7. 시간이 끝나면 정답과 점수를 공개한다.
8. 세션을 종료하고 결과 화면을 확인한다.

### 학생

1. 세션 코드를 입력한다.
2. 닉네임을 입력하고 대기실에 입장한다.
3. 교사가 시작하면 음악을 듣는다.
4. 4개 선택지 중 하나를 선택해 제출한다.
5. 정답 여부와 획득 점수를 확인한다.
6. 세션 종료 후 최종 점수를 확인한다.

## 3. MVP 포함 기능

### P0 - 반드시 구현

#### 세션 및 입장

- 교사 세션 생성
- 자동 세션 코드 발급
- 학생의 세션 코드 입장
- 닉네임 입력
- 학생 대기실
- 교사 화면의 참여자 목록

#### 음원과 문제

- MP3 1개 업로드
- 음원 재생
- 시작·종료 구간 설정
- 정답 1개와 오답 3개 등록
- 문제 제한 시간 설정
- 기본 점수 설정

#### 실시간 게임

- 교사 시작/일시정지/종료
- 학생 화면 타이머
- 학생 1회 응답
- 서버 기준 채점
- 제한 시간 종료 처리
- 교사 화면 실시간 응답 수 표시

#### 결과

- 학생 개인 정답 여부와 점수
- 교사 전체 정답률
- 최종 점수순 랭킹
- 세션 종료

### P1 - MVP 검증에 도움이 되는 기능

- 0.75배속 힌트 1종
- 정답 공개 전까지 학생 화면에서 정답 숨김
- 새로고침 후 세션 복구
- 문제 세션 저장 및 재사용
- 교사 화면에서 랭킹 숨김

### P2 - MVP 이후로 미룰 기능

- 가사·악기·초성 등 다양한 힌트
- 여러 문제로 구성된 플레이리스트
- 빠른 응답 보너스와 복잡한 점수식
- 팀전과 협동 모드
- 회원가입, 학교 계정, 장기 리포트
- 음원 자동 분석 및 AI 문제 생성
- 네이티브 모바일 앱

## 4. MVP 화면 목록

1. 교사 세션 설정
2. 교사 대기실/진행 화면
3. 학생 세션 입장
4. 학생 대기실
5. 학생 게임 화면
6. 학생 결과 화면
7. 교사 결과 화면

## 5. MVP 상세 요구사항 및 완료 조건

### 세션 생성

- [ ] 교사가 세션 제목을 입력할 수 있다.
- [ ] 생성 시 6자리 세션 코드가 발급된다.
- [ ] 세션 상태는 대기/진행/종료로 구분된다.

### 음원 업로드

- [ ] MP3 파일만 업로드할 수 있다.
- [ ] 업로드 후 파일명과 재생 시간이 보인다.
- [ ] 교사가 재생 시작/종료 초를 입력하거나 슬라이더로 설정할 수 있다.
- [ ] 업로드 실패 시 원인을 이해할 수 있는 메시지가 보인다.

### 학생 입장

- [ ] 학생은 코드와 닉네임만으로 입장할 수 있다.
- [ ] 학생이 입장하면 교사 화면의 참여자 수가 갱신된다.
- [ ] 세션이 시작되면 신규 입장을 제한한다.

### 게임 진행

- [ ] 교사가 시작 버튼을 누르면 모든 학생 화면이 게임 상태로 전환된다.
- [ ] 학생은 4개 선택지 중 하나를 선택해 제출한다.
- [ ] 제출 후 선택지를 다시 바꿀 수 없다.
- [ ] 제한 시간 종료 후 제출이 차단된다.
- [ ] 교사가 일시정지하면 학생 타이머와 음원도 함께 멈춘다.

### 채점 및 결과

- [ ] 정답은 서버에서 판정된다.
- [ ] 정답은 기본 점수, 오답은 0점으로 처리한다.
- [ ] 학생에게 문제별 점수와 정답 여부가 표시된다.
- [ ] 교사에게 응답자 수, 정답자 수, 정답률이 표시된다.
- [ ] 종료 후 학생별 총점과 랭킹이 표시된다.

## 6. 권장 MVP 기술 구조

- 프론트엔드: 반응형 웹 앱
- 백엔드: 세션·문제·응답 API
- 실시간 통신: WebSocket 또는 실시간 데이터베이스
- 파일 저장: MP3 업로드를 지원하는 오브젝트 스토리지
- 데이터베이스: 세션, 문제, 참여자, 응답 테이블
- 인증: MVP에서는 교사 임시 세션과 학생 닉네임 방식

핵심은 음원 자체를 학생마다 독립적으로 재생하는 것보다, 교사가 수업 상태를 제어하고 서버가 문제 상태·제출 마감·채점을 일관되게 관리하는 것이다.

## 7. 최소 API/이벤트 초안

### API

- `POST /sessions`: 세션 생성
- `POST /sessions/{code}/audio`: 음원 업로드
- `POST /sessions/{code}/questions`: 문제 생성
- `POST /sessions/{code}/participants`: 학생 입장
- `POST /sessions/{code}/start`: 게임 시작
- `POST /sessions/{code}/answers`: 답안 제출
- `POST /sessions/{code}/pause`: 일시정지
- `POST /sessions/{code}/finish`: 세션 종료
- `GET /sessions/{code}/results`: 결과 조회

### 실시간 이벤트

- `participant_joined`
- `session_started`
- `session_paused`
- `session_finished`
- `answer_submitted`
- `answer_result_revealed`
- `leaderboard_updated`

## 8. MVP 운영 시나리오

- 수업 전: 교사가 테스트용 MP3와 문제를 등록하고 학생용 참여 코드를 준비한다.
- 수업 시작: 프로젝터에는 교사 진행 화면, 학생 기기에는 학생 게임 화면을 띄운다.
- 문제 진행: 교사는 음량과 진행 상태를 확인하면서 시작/일시정지한다.
- 문제 종료: 정답률과 오답 분포를 보고 짧게 해설한다.
- 수업 종료: 결과 화면을 복습 자료로 활용하고 세션 데이터를 정리한다.

## 9. MVP 측정 지표

- 세션 생성 완료율
- 학생 입장 완료율
- 문제별 응답 참여율
- 평균 응답 제출 시간
- 문제별 정답률
- 재접속 및 오류율
- 세션 종료율
- 교사 재사용 의향
- 학생 재미/참여도 간단 설문

## 10. MVP 출시 전 체크리스트

- [ ] 교사 1명과 학생 5~10명으로 동시 접속 테스트
- [ ] 교실 Wi-Fi에서 음원 재생 및 타이머 동기화 테스트
- [ ] 새로고침·재접속 테스트
- [ ] 잘못된 파일 형식·긴 파일·빈 정답 테스트
- [ ] 제한 시간 경계 시점 제출 테스트
- [ ] 두 학생의 동일 닉네임 처리 테스트
- [ ] 세션 종료 후 재입장 차단 테스트
- [ ] 모바일/태블릿 가로 화면 테스트
- [ ] 교사가 음원을 사용할 권리가 있음을 확인하는 안내 추가

## 11. 4주 구현 순서 예시

### 1주차: 기반

- 화면 골격
- 세션 생성과 코드 입장
- 학생 대기실
- 기본 데이터 구조

### 2주차: 핵심 게임

- MP3 업로드와 재생
- 문제 설정
- 시작/일시정지/종료 상태
- 학생 답안 제출과 서버 채점

### 3주차: 실시간성과 결과

- 참여자 실시간 갱신
- 타이머 동기화
- 정답 공개
- 점수와 랭킹

### 4주차: 파일럿과 안정화

- 힌트 1종
- 재접속 복구
- 교실 파일럿 테스트
- 오류 수정과 사용성 개선

## 12. MVP의 종료 조건

다음 조건을 만족하면 MVP 검증 단계로 본다.

1. 교사가 5분 이내 한 곡 퀴즈를 생성할 수 있다.
2. 학생 10명이 동시에 입장해 한 문제에 응답할 수 있다.
3. 교사의 시작/일시정지/종료 상태가 학생 화면에 일관되게 반영된다.
4. 모든 응답의 정답 여부와 점수가 일치한다.
5. 세션 종료 후 교사와 학생이 결과를 확인할 수 있다.
6. 최소 2회 실제 수업 또는 모의 수업에서 핵심 흐름이 중단 없이 완료된다.
